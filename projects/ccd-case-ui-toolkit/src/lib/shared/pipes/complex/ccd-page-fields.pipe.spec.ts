import { CcdPageFieldsPipe } from './ccd-page-fields.pipe';

describe('CddPageFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new CcdPageFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
