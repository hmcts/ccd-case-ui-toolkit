import { CcdTabFieldsPipe } from './ccd-tab-fields.pipe';

describe('FieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new CcdTabFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
