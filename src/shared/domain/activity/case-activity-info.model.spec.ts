import { CaseActivityInfo } from './case-activity-info.model';

describe('activity.CaseActivityInfo', () => {
  it('should create an instance', () => {
    expect(new CaseActivityInfo()).toBeTruthy();
  });
});
